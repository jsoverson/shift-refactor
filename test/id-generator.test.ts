import { MemorableIdGenerator } from '../src/id-generator/id-generator';

const { expect } = require('chai');

xdescribe('MemorableIdGenerator', function () {
  it('should be tested', () => {
    // TODO: needs tests
  });
});
